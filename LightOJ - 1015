#include<bits/stdc++.h>
using namespace std;
int main()
{
int t,n,i,m,sum,j=0;
cin>>t;
while(t--)
{
    j++;
    sum=0;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>m;
        if(m>0)
        sum=sum+m;
    }
    cout<<"Case "<<j<<": "<<sum<<endl;
}
}
