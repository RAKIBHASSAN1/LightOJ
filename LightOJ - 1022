#include<bits/stdc++.h>
#define pi 2*acos(0.0)
using namespace std;

int main()
{
	double r,sarea,carea,area;
	int t,i;
    cin>>t;
    for (i = 1; i <= t; i++)
    {
		cin>>r;
		carea =pi* r * r;
		sarea = (4 * r * r);
		area = sarea - carea;
       printf("Case %d: %.2lf\n",i, area);
	}


}
