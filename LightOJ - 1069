#include<stdio.h>
int  main()
{
    int i,a,b,n,s;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d%d",&a,&b);
        if(a<b)
        s=(b*4+19);
        else
            s=((a-b)*4+(a-0)*4+19);
        printf("Case %d: %d\n",i,s);
    }
}
